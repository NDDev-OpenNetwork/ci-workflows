
aqtinstall.log
# aqtinstall writes this beside the working directory when the Qt fixture
dist/
.DS_Store
# is exercised locally. One got committed, so it is named here rather than
# left to be noticed again.
# Local virtualenvs
# macOS Finder metadata
*.pyc
__pycache__/
# Python bytecode / caches from the validator scripts
# Release build output
# Repository Python runtime, created by the launcher.
# Rust fixture build output (tests/fixtures/rust)
/.serena/
# Serena MCP local project config (developer tooling, not part of the published catalog)
tests/fixtures/rust/target/
tests/fixtures/rust/fuzz/artifacts/
tests/fixtures/rust/fuzz/corpus/
tests/fixtures/terraform/.terraform/
tests/fixtures/terraform/.terraform.lock.hcl
.venv/
